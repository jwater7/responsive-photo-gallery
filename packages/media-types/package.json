{
  "name": "rpg-media-types",
  "version": "0.0.0",
  "private": true,
  "description": "Single source of truth for supported media formats (extension sets, type predicates, MIME map, derived regexps) plus the shared excludes-aware directory walker every media-enumerating plane uses",
  "main": "index.js",
  "scripts": {
    "test": "node --test 'test/**/*.test.js'"
  },
  "license": "ISC"
}
