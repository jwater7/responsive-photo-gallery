{
  "name": "rpg-config",
  "version": "0.1.0",
  "private": true,
  "description": "Shared runtime-config for responsive-photo-gallery: CONFIG_PATH directory management, node-json-db config stores, the auth-path + legacy migration, and the directory-exclude contract shared by the gallery and the enrichment plane.",
  "main": "index.js",
  "scripts": {
    "test": "node --test 'test/**/*.test.js'"
  },
  "author": "j",
  "license": "ISC",
  "dependencies": {
    "debug": "^4.4.0",
    "node-json-db": "^2.3.0"
  }
}
